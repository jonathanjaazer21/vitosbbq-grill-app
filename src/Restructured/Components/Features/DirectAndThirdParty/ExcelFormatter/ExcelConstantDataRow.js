export const tableHeaders = ["CODE", "DESCRIPTION", "QTY", "PRICE", "TOTAL"]
export const tableData = ({ code, description, qty, price, total }) => {
  return [code, description, qty, price, total]
}
