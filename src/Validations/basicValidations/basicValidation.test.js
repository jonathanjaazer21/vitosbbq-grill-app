import { modifiedValidation } from "."

test("[Validations/validateAuth] validateAuth() with null password", () => {
  // expect(validateAuth({ username: "jonathan", password: "" })).toStrictEqual({
  //   valid: false,
  //   error: "password",
  //   message: "This is required",
  // })
})
